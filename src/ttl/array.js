const fruits = ['apple', 'banana', 'graph'];


fruits.forEach((fruit, index) => {
    console.log(fruit, index)
})

fruits.pop();

fruits.forEach((fruit, index) => {
    console.log(fruit, index)
})

